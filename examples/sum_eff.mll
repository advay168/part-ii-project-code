let numsGen := fun u ->
  let u := perform (1) in
  let u := perform (2) in
  let u := perform (3) in
  let u := perform (4) in
  0
  end
  end
  end
  end
end
in
let sum := fun gen ->
  handle
    gen@0
  with
    v k -> v + sum@k
  end
end
in
  sum@numsGen
end
end
